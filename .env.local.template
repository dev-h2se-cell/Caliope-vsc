# -----------------------------------------------------------------------------
# Firebase Client-Side Configuration (NEXT_PUBLIC_)
# Estas variables son PÚBLICAS y se usan en el navegador.
# Copia estos valores desde la configuración de tu app web en la consola de Firebase.
# -----------------------------------------------------------------------------
NEXT_PUBLIC_FIREBASE_API_KEY="AIza..."
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="tu-proyecto.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="tu-proyecto"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="tu-proyecto.appspot.com"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="..."
NEXT_PUBLIC_FIREBASE_APP_ID="1:..."
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID="G-..."

# -----------------------------------------------------------------------------
# Demo Mode Flag (Opcional)
# Poner esto en 'true' forzará a la aplicación a ejecutarse en modo demo,
# usando un usuario y perfil falsos, sin necesidad de conectarse a Firebase.
# Ideal para desarrollo de UI sin conexión.
# -----------------------------------------------------------------------------
NEXT_PUBLIC_DEMO_MODE="false"

# -----------------------------------------------------------------------------
# Firebase Server-Side Admin Configuration (SECRETA)
# ¡NUNCA compartas esta variable ni la subas a un repositorio público!
# Genera una clave de cuenta de servicio en la consola de Firebase:
# (Project Settings > Service accounts > Generate new private key)
# Luego, copia el contenido COMPLETO del archivo JSON aquí, en una sola línea.
# -----------------------------------------------------------------------------
FIREBASE_SERVICE_ACCOUNT=""
